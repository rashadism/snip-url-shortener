apiVersion: openchoreo.dev/v1alpha1
kind: Component
metadata:
  name: snip-api-service
  namespace: default
spec:
  owner:
    projectName: url-shortener

  componentType:
    kind: ClusterComponentType
    name: deployment/service

  autoDeploy: true

  workflow:
    kind: ClusterWorkflow
    name: dockerfile-builder
    parameters:
      repository:
        url: "https://github.com/rashadism/snip-url-shortener"
        revision:
          branch: "v18"
        appPath: "/api-service"
      docker:
        context: "/api-service"
        filePath: "/api-service/Dockerfile"

---
apiVersion: openchoreo.dev/v1alpha1
kind: WorkflowRun
metadata:
  name: snip-api-service-build-01
  labels:
    openchoreo.dev/project: "url-shortener"
    openchoreo.dev/component: "snip-api-service"
spec:
  workflow:
    kind: ClusterWorkflow
    name: dockerfile-builder
    parameters:
      repository:
        url: "https://github.com/rashadism/snip-url-shortener"
        revision:
          branch: "v18"
        appPath: "/api-service"
      docker:
        context: "/api-service"
        filePath: "/api-service/Dockerfile"
